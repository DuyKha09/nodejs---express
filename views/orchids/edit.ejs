<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Orchid</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>

<div class="container-fluid">
  <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">Editing: <%= orchid.name %> <%= orchid.lastName %></h1>
  </div>

  <div class="col py-3">
    <div class="row">
      <div class="col">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/orchids">Dashboard</a></li>
            <li class="breadcrumb-item active"><%= orchid.name %> <%= orchid.lastName %></li>
          </ol>
        </nav>
      </div>
    </div>
  </div>

  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <img src="<%= orchid.image %>" class="img-fluid" alt="<%= orchid.name %>">
      </div>
      <div class="col-md-6">
        <h2><%= orchid.name %></h2>
        <form id="updateForm" action="/orchids/<%= orchid._id %>?_method=PUT" method="POST">
          <div class="form-group row">
            <div class="col">
              <label for="name"><b>Orchid's Name</b></label>
              <input type="text" class="form-control" id="name" name="name" value="<%= orchid.name %>" placeholder="Enter name" required>
            </div>
            <div class="col">
              <label for="price"><b>Price</b></label>
              <input type="number" min="0" class="form-control" id="price" name="price" value="<%= orchid.price %>" placeholder="Enter price" required>
            </div>
          </div>
          <div class="form-group row">
            <div class="col">
              <label for="original"><b>Original</b></label>
              <input type="text" class="form-control" id="original" name="original" value="<%= orchid.original %>" placeholder="Enter original" required>
            </div>
            <div class="col">
              <label for="color"><b>Color</b></label>
              <input type="text" class="form-control" id="color" name="color" value="<%= orchid.color %>" placeholder="Enter color" required>
            </div>
          </div>
          <div class="form-group">
            <label for="image"><b>Image</b></label>
            <input type="text" class="form-control" id="image" name="image" value="<%= orchid.image %>" placeholder="Enter image URL" required>
          </div>
          <div class="form-group">
            <label><b>Is Natural:</b></label><br>
            <div class="form-check form-check-inline">
              <input type="radio" class="form-check-input" id="trueRadio" name="isNatural" value="true" <%= orchid.isNatural === true ? 'checked' : '' %>>
              <label class="form-check-label" for="trueRadio">True</label>
            </div>
            <div class="form-check form-check-inline">
              <input type="radio" class="form-check-input" id="falseRadio" name="isNatural" value="false" <%= orchid.isNatural === false ? 'checked' : '' %>>
              <label class="form-check-label" for="falseRadio">False</label>
            </div>
          </div>
          <div class="form-group">
            <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#confirmModal">Update Orchid</button>
            <a href="/orchids" class="btn btn-secondary">Close</a>
          </div>
        </form>
      </div>
    </div>
  </div>

</div>

<!-- Modal -->
<div class="modal fade" id="confirmModal" tabindex="-1" role="dialog" aria-labelledby="confirmModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="confirmModalLabel">Confirm Update</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Are you sure you want to update this orchid?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
        <button type="submit" form="updateForm" class="btn btn-primary">Update</button>
      </div>
    </div>
  </div>
</div>

<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
